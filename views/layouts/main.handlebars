<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous" />
    <title>Tech Blog Site</title>
</head>

<body>
    <header>
        <nav class="navbar navbar-dark bg-dark d-flex flex-column align-items-start pl-5 mb-4">
            <ul class="navbar-nav d-flex flex-row">
                <li class="nav-item p-3">
                    <a class="nav-link" href="/">Home</a>
                </li>
                {{#if logged_in}}
                <li class="nav-item p-3">
                    <a class="nav-link" href="/profile">Dashboard</a>
                </li>
                <li class="nav-item p-3">
                    <a class="nav-link logout">Logout</a>
                </li>
                {{else}}
                <li class="nav-item p-3">
                    <a class="nav-link" href="/login">Login</a>
                </li>
                {{/if}}
            </ul>
            <h2 class="mx-3 px-3 rounded-lg text-dark bg-white">The Tech Blog</h2>
        </nav>
    </header>
    <main class="d-flex flex-column justify-content-center col-12">
        {{{body}}}
    </main>

    {{#if logged_in}}
    <script src="/logout.js"></script>
    {{/if}}
</body>

</html>